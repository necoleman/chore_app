# List of enhancements and bugfixes

Rev. 2026-07-05
App version 1.4.0

## Issue log

This is a list of issues not covered in the previous test plan. The 
ID | Platform | Permissions level | Browser | Issue | Resolution
-|-|-|-|-|-
1 | iOS | Admin | Safari | After "Add" is clicked, the menu modal at the bottom of the screen obscures the "Cancel" and "Save" buttons. This is not an issue in Android (Vivaldi browser). | Resolved (v0.1.2) — modals now render via a portal at `<body>`, escaping the scroll container that clipped them on iOS
2 | iOS | Admin | Safari | When opening from the home screen, text and buttons at the top of the page are obscured by the iphone status such as battery, making it impossible to click "Add". This is not an issue in Android (Vivaldi browser) | Resolved (v0.1.2) — added `env(safe-area-inset-top)` padding to the app shell
3 | iOS | Admin | Safari | If you click the three dots to view the options on a chore and then click out of the three dots it marks the chore as complete. This shouldn't happen. Ideally, a chore should be marked as complete only if checking the circle where the check will go - not the chore name or anywhere on the chore other than that circle. | Resolved (v0.2.2) — completion is now triggered only by the circle (its own button); the card is no longer clickable
4 | iOS | Admin | Safari | Clicking "move date" or "reassign" in the three dot drop down does not give you the date screen or reassign screen when you click on them for a chore assigned to me - it only works if I click on them for a chore assigned to someone else. Needs fixing! | Resolved (v0.2.2) — same root cause as #3 (menu taps bubbled to the card's complete handler); fixed by removing the card handler + `stopPropagation` on the menu items
5 | iOS | Admin/User | Safari | When admin approves or does not approve a chore marked as complete by a child user, they are allowed to add a comment if marking to send the chore back - however, it does not appear that that comment is then displayed to the child user. The child user should have the chore unchecked in their bucket again (as currently happens) but should also see the feedback. Maybe the chore should have on it text like (example is for clean bathroom task): Chore sent back by admin user: The floor is still dirty, please reclean it. Everything else is fine. | Resolved (v0.2.2) — reject sets status `open` (not `rejected`), so the note was never shown; the card now displays "Sent back by {reviewer}: {note}" whenever a review note is present, cleared when the child re-completes
6 | iOS | Admin | Safari | Figure out what "Did it" means on the Chores screen - seems to mean nothing. I think it's not necessary. I think just having edit is ok. | Resolved (v0.2.2) — "✓ Did it" removed entirely (button + `log_done` endpoint); Manage Chores rows now show only Edit
8	| iOS | admin |	safari | When adding a weekly chore, it isn't possible to type anything into the "Weekday" field - probably because there isn't a header for that in the Chores tab. This seems to prevent it from being added to Assignments (it does appear in the Chores tab, but not in Assignments and does not appear in Today on the app).	| Resolved (v1.3.1) — a reactive `$: form.custom_days = selectedDays.join(',')` re-fired on every form change (incl. typing the weekday, which also binds `custom_days`), wiping it → blank `custom_days` → generator never matched. Sync moved into `toggleDay` (custom path only); the weekly field is now a Sunday–Saturday dropdown.
9	| iOS | admin |	safari | When I create an interval tab and set it to first be due on July 5th, it shows up properly in the Chores tab but is not added to Assignments and thus does not appear on the Today tab in the app.| Resolved (v1.3.1) — the generator was correct (it creates the assignment on/after `start_date`), but the hard start guard suppressed the lead window. `start_date` is now the first occurrence's due date and the lead window applies, so the chore surfaces early (e.g. as "Due Jul 5") instead of only appearing on the day.
10	| iOS | admin |	safari | On the Today screen, hitting "Tap to claim" doesn't do anything. On desktop and android, chores can be claimed by tapping the place where checkmarks happen. On ios safari chores don't appear to be claimable at all.	| Resolved (v1.3.1) — the "Tap to claim" text was a passive label; it's now a real button. Also added `type="button"` to the circle and `pointer-events:none` to the decorative check SVG so iOS reliably fires the tap. *(Verify on device.)*
11	| iOS | admin |	safari | Assignment and chores tabs are not synced. When a chore is deleted, the assignments that refer to it need to be deleted.	| Resolved (v1.3.1) — `actionToday`/`actionHistory` now skip assignments whose `chore_id` has no matching Chores row, so deleting a chore's row makes its "ghost" cards disappear. (Read-side only — no overnight deletions, to avoid a sheet typo cascading into data loss.)
12 | iOS | admin | safari | When I change the first due date of an Every N Days chore in the Chores tab of the app it doesn't update on the Today tab. I see in the Assignments tab in this spreadsheet that the due date doesn't change even though it does update in the Chores tab in this spreadsheet. Something isn't translating through. | Resolved (v1.3.3) — the interval generator is anchored on `last_generated_date`, so editing `start_date` only rewrote the Chores row and never moved the already-generated assignment. `actionUpdateChore` now re-anchors: when an interval chore's `start_date` changes and it has no activity yet (exactly one open, auto-generated assignment), it moves that assignment's `due_date` to the new date and resets `last_generated_date`. Chores already in progress are left alone (no rescheduling of started work).
13 | iOS | admin | safari | When I create "Every N Days" chores and set first due dates of today or tomorrow, they correctly show in the Today tab with "Due Today" or "Due Tomorrow." However, in the Chores tab, they show as "Next: Oct 3" and "Next: October 4". These dates are 90 days (the interval I set) after the first due date for each. I'm not sure why they're showing that way in the Chores tab, when they're actually showing correclty in the Today tab. Note that the monthly and weekly chores I create and set to first be due today show "Next: Today" in each tab so it's not that the Chores tab is actually showing the next due date for all non-daily chores that are due today or some such. This only seems to impact Every N Days chores. | Resolved (v1.3.3) — `nextDueDate`'s interval branch returned `last_generated_date + N`, but generation stamps `last_generated_date` to the current occurrence's due date, so a chore due today read as "today + interval". It now treats `last_generated_date` as the current occurrence and advances by whole intervals only to the first date on/after today — so a chore due today reads "Today" (matching weekly/monthly), and a past one rolls to its next real date.
14 | iOS | admin | safari | When a chore is overdue and I check it off, it disappears from my list rather than being grayed out and moving to the bottom. I think this is because it's past due. However, any chore checked off as done today should stay in my list (checked off and shaded etc) for the duration of that day. | Resolved (v1.3.3) — the Today filter (both `actionToday` and `filterTodayAssignments`) kept done/skipped items only when `due_date === today`, so an overdue chore vanished the moment it was checked off. Done items are now also kept when they were **completed today** (`completed_at` is today), so they stay greyed at the bottom for the rest of the day. Skipped items keep the due-today rule (a skip records no timestamp).


## Enhancements

Here is a list of proposed enhancements:

| Status | ID | Enhancement |
|-|-|-|
| ✅ Done (v0.1.2) | 2 | Have a "Chore Location" category feature that shows up on the chore - this way there can be multple chores with the name Vacuum and each can have a category (kitchen, living room, etc) - only need one location per chore. Chores can then also be sorted and organized by category. *(Location dropdown fed by `Locations` sheet tab; shown on chore rows and Today cards; searchable. Sorting/grouping by category not yet added.)* |
| ✅ Done (v0.1.2) | 3 | Have a "Description" column where Admins can enter more information about what the chore involves (additional granularity) - for example, this way the chore "clean bedroom" can have notes in it saying "this means sweep, make surfaces orderly, make bed, clear floor, and vacuum |
| ✅ Done (v0.1.2) | 4 | Have the ability to search the full chore list, so that if I go through the closet on the landing and reorganize it even if it wasn't assigned, I can check whether it exists and mark that I did it and change the frequency if I feel it needs changing, or if I don't find it in the list I can then add it as a chore (but this way I don't add it twice). *(Search matches name/location/description/assignee/"Unclaimed"; Edit changes frequency; add-from-search when no match. Note: the "✓ Did it" ad-hoc log was later removed per issue 6.)* |
| ✅ Done (v0.2.0) | 5 | When a chore is approved (or is done and does not require approval), it goes to the bottom of the day's chore list and is grayed out there. More precisely, the chores are sorted so that all open/unapproved chores are above all closed/approved chores. Also make chores pending approval a different color (eg amber) *(Finished chores sort to the bottom; among unfinished, overdue first. Pending shows amber; the assignee now sees their own pending as a "Waiting for review" card.)* |
| ✅ Done (v0.2.0) | 6 | Uncheck a chore, in case you realize you checked it by mistake. Make sure that an unchecked chore has a confirmation. Approved chores cannot be unchecked. (Unchecked chores should also move back up.) *("Undo" button → confirmation modal; reverts to open, removes awarded points (clamped ≥0), moves back up. Approved chores (reviewed_by set) have no Undo and are blocked server-side.)* |
| ✅ Done (v0.2.0) | 7 | Keep overdue chores from previous days on the "Today" tab flagged as overdue until they're either resolved or bumped by an admin *(Unfinished items of any age stay on Today with an "Overdue · MM-DD" badge, sorted to the top; cleared by completing, bumping, or skipping. No age cutoff.)* |
| ✅ Done (v0.2.0) | 8 | Handle one-time tasks (for example, moving furniture in/out). It's fine to update the data model for this. *(New `once` frequency + `once_date` column; generates a single assignment on/after the date, then auto-archives the chore (active=FALSE). Form has a "One-time" option + date picker.)* |
| ✅ Done (v1.0.0) | 9 | When adding a new task as Monthly or Every N Days, there should be a way to set "first due date" - for a new task, it should decay or to today's date but should be editable. In general, tasks should have a "next due" and " last done" field. For non weekly daily tasks it could make sense to have "next due" listed so it is shown alongside assignee and location, without having to click on the task. *(Largely covered by #13 (`start_date` "first due date") and #10/#14 (computed "Next:" tag). Added here: a "Last done: …" tag on the Chores screen, computed in `actionChores` from the most recent done assignment per chore — no stored column.)* |
| ✅ Done (v0.3.0) | 10 | In the Chores screen (but NOT in the Today screen), you should be able to see when the next date the chore will be due, except for daily chores (which should not display a date due). Maybe: Weekly chores or chores that are scheduled to occur several specific days of the week should either say "Today" if they are due today or the name of the weekday they are next due (i.e. Tuesday) rather than the date. |
| ✅ Done (v0.3.0) | 11 |In the Chores screen, you should be able to see the first line of the description of the chore, with a little button that opens the rest of the description so you can see the full thing, and a button to minimize it again. This should probably also be the case on the Today screen, although we need to be careful that the description doesn't clutter things up too much. Maybe it's just a button with a carrot at the bottom of each chore that says "Description" and if you hit the carrot it opens, and hten you hit a minimize carrot to close it again? |
| ✅ Done (v0.3.0) | 12 | It should be possible to (from the Today screen) reassign a chore to the unclaimed button (i.e. reassign it from you or someone else without having to choose a new person to assign it to, so it instead goes back in the unclaimed bucket) *(Three-dot menu → "↩ Make unclaimed".)* | 
| ✅ Done (v0.3.0) | 13 | Set the first date a chore is to be due, in the chore editor *(new `start_date` column; the generator won't run the chore before it).* |
| ✅ Done (v0.3.0) | 14 | Next date expected to be displayed in the Chores screen *(same as #10 — a "Next: …" tag per non-daily chore).* |
| ✅ Done (v0.3.0) | 15 | Ability to sort the Chores screen by location, default person (unclaimed at top), by periodicity, or by countdown time *(Sort dropdown: Default / Location / Assignee / Periodicity / Next due).* |
| ✅ Done (v1.0.0) | 16 | Choose the weekend that a monthly chore is done. Monthly chores should, instead of date number of each month, instead be set to come due “first Friday of the month” or second or third or fourth. So when you create, you choose from those options: First Friday, Second Friday, Etc. Disregard the existence of the fifth Friday. *(New `monthly_week` (1–4) + `monthly_weekday` (0–6) columns; the chore editor's monthly section has a "Day of month / Day of week" toggle. When both nth-weekday fields are set they take precedence over `monthly_day`. Day-of-month monthly still supported.)* |
| ✅ Done (v1.0.0) | 17 | Chores created on a day and set to be due that day should show up. Currently a chore created with due date today does not show up on the assignment screen *(add/update-chore now generate today's assignment immediately via a shared `generateAssignmentIfDue` helper, instead of waiting for the nightly run.)* |
| ✅ Done (v1.0.0) | 18 | Color-code assignments by frequency: daily, light blue; weekly, light green; all others, light yellow *(Whole-card tint on open cards only; pending (amber) / rejected (red) and done/skipped greying still override. `frequency` added to the Today payload.)* |
| ✅ Done (v1.1.0) | 19 | Quick-create a chore from the Today screen. Any user should be able to do this - it should default to a one-time chore. Admins should be able to edit the chore to make it recurring from the Chores screen. *("+ Add" on the Today header opens a name-only sheet (`QuickAddChore`). Creates a `once` chore due today, assigned to the creator, 1 pt, no approval — reusing `add_chore` + the #17 generate-on-create helper. The chore auto-archives into Manage Chores → Inactive, where an admin can edit it into a recurring, active chore. Follow-up pinned: discoverability/cleanup if one-time chores pile up.)* |
| ✅ Done (v1.2.0) | 20 |	Quick-add has an assignment field for admins *(Admins get an "Assign to" selector on the Today quick-add sheet — Me / any person / Unclaimed; non-admins still self-assign only.)* |
| ✅ Done (v1.3.0) | 21 |	Overdue chores have not just overdue and due date, but how many times they've been missed. So instead of a daily chore missed three days in a row resulting in three overdue assignments, it results in one assignment that's overdue by three days. (Backend change in daily update - if an assignment is already open for a chore to be assigned, increment the "overdue by" field.) This should also _decrement_ the leaderboard by the point value of the chore. *(v1.2.0 shipped the display groundwork; v1.3.0 rewrote the generator: when the next occurrence comes due and the prior one is still open, it keeps ONE assignment, bumps `missed_count`, and deducts the chore's points from the assignee once per recurrence (clamped ≥0). Days-overdue is computed on the card from `due_date`.)* |
| ✅ Done (v1.2.0) | 22 |	Sort options the assignments page - by due date, by frequency *(Today screen "Sort: Default / Due date / Frequency" dropdown; sorts within the My / Family / Available sections, finished items stay last.)* |
| ✅ Done (v1.3.0) | 23 |	For non-daily chores, have them able to appear some days before their due date. This allows weekend chores to appear on Thursday, for example. We'll have default durations (e.g. weekly chores have four days to be done, due date Sunday, so they appear on Thursday morning; monthly chores have seven days to be done, due date a Sunday, so they appear the previous Monday morning and are in the list all week) *(v1.3.0 generator now creates each occurrence on its appear date (`due − (lead_days − 1)`) with the real future due date; the Today filter surfaces it once appeared. `lead_days` = "days to complete" (inclusive window), per-chore with frequency defaults (weekly/custom 4, monthly 7, interval min(N,7), daily/once 1), editable in the chore editor. **Note the interpretation: `lead_days=4` weekly due Sunday → appears Thursday, matching the examples.**)* *(v1.3.2 refined the rules: `lead_days` = days visible before overdue; must be ≥1 and **strictly less than the recurrence interval**; **defaults to 1** for all frequencies (early appearance is now opt-in per chore — the old weekly-4/monthly-7 auto-defaults were dropped). This also fixed a short-interval bug where a chore reappeared the next day / penalized ~1 day after due instead of after a full interval.)* |
| ✅ Done (v1.4.0) | 24 | Chores should be able to rotate between people. The idea is that if the default_assignee field in the chore is a comma-delimited list of person_ids then the next assignment of that chore goes to the next person in the list from the previous assignee of the chore. If there's no previous assignmnet, then it defaults to the first person in the list. *(Generator rotates when `default_assignee` is a comma list: each fresh occurrence goes to the person after the last **planned** assignee, wrapping around. Rotation state lives in a new internal `rotation_last` Chores column — never read from the assignment's `person_id` — so a manual reassignment doesn't shift the order (the next occurrence still goes to whoever would have gotten it). An empty/absent pointer, or one whose person was dropped from the list, falls back to the first person. Single-person and empty lists behave as before; carry-overs don't advance the pointer. Manage Chores shows the rotation as "A → B → C". Backend + display only — set the comma list directly in the Chores sheet.)* |
| ✅ Done (v1.5.0) | 25 | For a pending daily chore whose assignment yesterday was neither approved or sent back yesterday, create the new assignment for today as if yesterday's assignment was done. If the pending assignment is approved, then there is no change to today's assignment as the assignment was done yesterday. However, if if the pending assignment is sent back, then the assignment from yesterday is registered as overdue and today's assignment of the chore is handled per: (a) if today's asisgnment of the chore is open, it is deleted; (b) if today's assignment of the chore is done or pending, then no change is made to it; the work is already done *(No generation change needed: a `pending_review` prior occurrence already doesn't trigger the generator's open-prior collapse, so today's fresh assignment is created "as if done." The work is in `actionReject`: after reopening the sent-back assignment (which renders it overdue), it reconciles NEWER occurrences of the same **daily** chore — deleting open ones (new `deleteRow` helper), leaving done/pending ones. Approve is unchanged. Scoped to `frequency === 'daily'`; other cadences rely on the existing lead-window collapse.)* |
| ✅ Done (v1.5.0) | 26 | Visualization update. Make the other users' assignments into a drop-down and close them by default, so by default each user sees only their own chores and the unclaimed chores. *(The "Family" section on Today is now a single native `<details>` dropdown, collapsed by default, with a summary showing a live chore count. My Chores and Available-to-Claim stay expanded.)* |
| ✅ Done (v1.5.0) | 27 | Visualization update. Sort assignments so that, for each person, overdue chores are at the top of their assignment list, and chores due in the future are at the bottom of their assignment list. *(The default section comparator in `choreSelectors.js` now applies a due-date-ascending tiebreak within each rank — overdue (earliest) at top, future at bottom, finished still last — for My Chores, each family group, and the now-sorted Available-to-Claim section.)* |
| ✅ Done (v1.6.0) | 28 | Visualization update. Within each person's assignment list, segment the cards for assignments that are overdue or due into a sublist labeled "Today", and segment the chores that are not due today into a second collapsible sublist labeled "Due soon" *(New `partitionByDue` selector splits each already-sorted list into "today" (overdue/due-today, plus finished-today) and "soon" (unfinished + future due date). A new `AssignmentSegments.svelte` renders the today cards, then a collapsed `<details>` "Due soon (N)" fold when any future cards exist; the "Today" label only shows when both buckets are present. Applied to My Chores and each Family group; Available to Claim stays fully open. Display only — no backend change.)* |
| ✅ Done (v1.7.0) | 29 | Vacation mode. An admin can set a person as "on vacation." When this is triggered, all of their open assignments and any chores that default to them will instead go to unclaimed. Rotating chores skip them in the rotation. When they are taken off vacation mode, this immediately triggers recalculation of current assignments and reassigning their default assignments to them. *(New `on_vacation` People column, toggled from Manage Chores → a new "People" section ("Set vacation" / "End vacation"). Turning ON moves the person's open assignments to unclaimed (done/pending left alone). Turning OFF re-homes currently-unclaimed open assignments whose chore's **sole** default assignee is that person back to them (a targeted re-home, not a full generation run — nothing is pulled forward); shared/rotation chores stay unclaimed and get picked up on the next occurrence. `resolveRotationAssignee` now takes `people` and skips vacationers: a sole assignee on vacation → unclaimed, a rotation steps past them, all-on-vacation → unclaimed.)* |
| ✅ Done (v1.7.0) | 30 | Add a new attribute to each chore: "Rollover" or "Recreate." This attribute governs the behavior of a recurring chore if the assignment is not complete when the chore recurs. The current expected behavior for an incompleted recurring chore is what I'm calling "rollover": a new assignment is not created, and the existing assignment is marked overdue. We're going to add new behavior: "Recreate." The existing assignment is marked overdue and left open, however a new assignment is _also_ created. This is appropriate for chores that compound, like dishes: If one doesn't finish the dishes after dinner, one still needs to do them in the morning, and then _again_ in the evneing. *(New `recur_mode` Chores column: `rollover` (default/blank) keeps the #21 collapse+penalty; `recreate` skips the *collapse* so the still-open prior stays overdue and a fresh occurrence is created on top — but the missed occurrence is **still penalized** (points deducted once, `missed_count` bumped), penalizing the just-superseded occurrence so each is flagged exactly once. Shared penalty logic extracted to `penalizeMissedOccurrence`. Chore editor gets an "If not done when it recurs" selector. Also suppresses the #25 daily-reject reconciliation for `recreate` chores, which legitimately keep multiple open rows.)* | 
| ✅ Done (v1.8.0) | 31 | Issue: When chores are complete, some still show as "Done" even the next day. They should not display. *(Root cause: `nowIso()` stamped `completed_at`/`reviewed_at` in **UTC** (`toISOString()`) while every "today"/due comparison is **local** (script/device timezone). In a behind-UTC zone an evening completion got the *next* day's date, so the Today filter's "completed today" test (`completed_at.slice(0,10) === today`) matched the following day and the greyed card lingered. Fixed in two parts: (1, v1.8.0) `nowIso()` now emits a script-timezone ISO datetime, so new `completed_at` values' date portion matches `todayStr()`/`formatDate()`; (2, v1.8.1) the `actionToday` "completed today" test now compares the completion **instant** (`new Date(completed_at)` reformatted in the script tz via `completedOnLocalDate`) instead of slicing the raw stored string — so **legacy UTC timestamps already in the sheet** also resolve to their correct local day and clear immediately, rather than lingering a day until the fix propagated. All five audit-timestamp call sites are unchanged.)* |
| ✅ Done (v1.8.0) | 32 | Issue: When recurring chores _had_ been overdue but are done, they still display as overdue after recurring. At that point, they should display as usual, as new assignments. *(Same UTC-vs-local root cause as #31: the stale, greyed done occurrence lingered an extra day alongside the fresh recurrence, and its `missed N×` badge made it read as still-overdue. Fixed by the `nowIso()` change (the stale row now drops off at the correct local midnight) plus gating the `missed_count` badge on unfinished cards in `ChoreCard.svelte` (a done card no longer shows "missed N×").)* |
| ✅ Done (v1.8.0) | 33 | New feature: Give admins button to reset rotating assignments to the first set of assigned. This should change all existing current (ie not overdue) assignments. *(New `reset_rotation` action + a per-chore "Reset rotation" button on rotating chores in Manage Chores. Reassigns the chore's **current** (open, `due_date >= today`) assignments to the first person in the `default_assignee` list and sets `rotation_last` to that person so the next occurrence restarts the sequence at the second person. Overdue open assignments are deliberately left untouched.)* |
| ✅ Done (v1.8.0) | 34 | Issue: "So-and-so was on vacation mode for a week. When I ended vacation mode, I found that chore ID clean_bathroom_511 assignment ID clean_bathroom_511_20260721 is showing as assigned to So-and-so and due 7/21/2026 and missed 4 times. Why would this be? If it had been missed four time when she left and THEN paused, I would expect it to show as missed four times and due either today (since she just got back) or due four days before vacation mode started. Why would it be showing as due 7/21, in the middle of her vacation period?" *(Root cause: the nightly generator was only vacation-aware when picking an assignee — it still ran for a sole-vacationer's chore, creating an unclaimed row dated mid-vacation (7/21) and then bumping its `missed_count` (→4) on each later occurrence via the rollover collapse, all with a frozen due date; ending vacation re-homed that stale row to the person verbatim. Fixed with a **vacation-pause** guard in `processChoreGeneration`: when a chore's default assignee(s) are ALL on vacation, the occurrence is consumed (anchor advances so dates don't pile up) but **no row is created and no penalty/`missed_count` bump happens**. On return, the pre-vacation occurrence carries over unchanged — its original due date and miss count, no mid-vacation dates. Genuinely-unclaimed chores and partially-available rotations are unaffected.)* || ✅ Done (v1.8.2) | 35 | Issue: three related symptoms — (a) checking off an overdue chore makes it regenerate the next day *still* overdue; (b) a chore toggled `active` FALSE → TRUE regenerates with past due dates; (c) chores reappear with dates from during a vacation. *(One root cause: `last_generated_date` is a **schedule cursor**, and `nextDueForChore` returned the occurrence immediately after it without ever comparing to today. The cursor freezes entirely while a chore is inactive (`processChoreGeneration` returns before advancing it) and lags after any missed nightly run, so the "next" occurrence was itself in the past and got written straight into `due_date` — the assignment was **born overdue**, and the cursor then crawled forward one occurrence per night, deducting points each time. Symptom (a) is the same lag becoming visible: while a row is open the rollover collapse suppresses new rows and hides the drift; completing it lets the next run emit the stale occurrence. Fixed by clamping `nextDueForChore` to today: `interval` rolls forward by whole intervals (preserving cadence phase, matching the frontend's `dueDates.js`, which always did this), calendar frequencies resume at the next scheduled day on/after today, and a back-dated `once_date` surfaces today. Clamping collapses any backlog in a **single** run rather than one night per occurrence. Also hardened the `interval_days` guard to reject non-positive values, which would otherwise spin the roll-forward loop. No data migration — frozen cursors self-heal on the first run.)* |
| ✅ Done (v1.8.2) | 35 | Issue: three related symptoms — (a) checking off an overdue chore makes it regenerate the next day *still* overdue; (b) a chore toggled `active` FALSE → TRUE regenerates with past due dates; (c) chores reappear dated during a vacation. *(One root cause: `last_generated_date` is a **schedule cursor**, and `nextDueForChore` returned the occurrence immediately after it without ever comparing to today. The cursor freezes entirely while a chore is inactive (`processChoreGeneration` returns before advancing it) and lags after any missed nightly run, so the "next" occurrence was itself in the past and got written straight into `due_date` — the assignment was **born overdue**, and the cursor then crawled forward one occurrence per night, deducting points each time. Symptom (a) is that same lag becoming visible: while a row is open the rollover collapse suppresses new rows and hides the drift; completing it lets the next run emit the stale occurrence. Fixed by clamping `nextDueForChore` to today — `interval` rolls forward by whole intervals (preserving cadence phase, matching the frontend's `dueDates.js`, which always did this), calendar frequencies resume at the next scheduled day on/after today, and a back-dated `once_date` surfaces today. Clamping collapses any backlog in a **single** run rather than one night per occurrence. Also hardened the `interval_days` guard to reject non-positive values, which would otherwise spin the roll-forward loop. No data migration — frozen cursors self-heal on the first run.)* |
| ✅ Done (v1.8.2) | 36 | Change: `interval` chores should count from when the chore was actually done, not from its scheduled due date. Doing a 90-day chore 30 days late previously left the next occurrence only 60 days out, because the cursor was stamped with the occurrence's due date at generation time. *(New `anchorIntervalOnCompletion` helper stamps `last_generated_date` with the completion date whenever an interval assignment reaches `done` — from `actionComplete` (immediate-done branch only, not `pending_review`) and from `actionApprove`, which anchors on `completed_at` rather than the review timestamp so a late review doesn't push the cadence out. `actionUncomplete` restores the anchor to the occurrence's own due date. Scoped strictly to `frequency === 'interval'`; calendar frequencies must stay locked to their calendar, so completing one early or late does not shift it. Idempotent — completing on the due date costs no sheet write.)* |
| ✅ Done (v1.8.2) | 37 | Issue (#34 follow-up): coming back from vacation still surfaced chores as overdue. The v1.8.0 vacation-pause guard stopped the generator creating mid-vacation rows, but rows that were already in flight when the vacation began kept their original due dates, aged the whole trip in the unclaimed pile, and were handed back verbatim on return. *(Ending vacation now **re-dates** a re-homed row to today and clears its `missed_count` when its due date has passed — nobody could have done it — while a row still due today or later keeps its date. Mirrors the `due_date >= today` guard `actionResetRotation` already had. Separately, rejecting a chore whose owner is away now parks it in **unclaimed**: turning vacation ON only sweeps rows that were already `open`, so a `pending_review` row sent back mid-trip slipped past it and sat stranded on the vacationer's list. The reject response also clears the denormalized `person_name`/`person_color` so the card updates immediately instead of showing the absent owner until the next 30s poll.)* |
| ✅ Done (v1.9.0) | 38 | Rework: replace the rollover/recreate recurrence model. A chore should never sit frozen at an old due date — if it isn't done by the time it comes round again, subtract the points and regenerate it due on its next real date, showing a running count of how many times it's been missed since it was last completed. *(`processChoreGeneration` rewritten as a **close-and-create roll-forward**: at the regeneration date the live occurrence is closed as `skipped` with the points deducted and recorded as a **negative `points_awarded`** (that sign is what lets History tell an automatic miss from an admin excusal, which records none), and a fresh occurrence is created carrying `missed_count + 1`. `recur_mode` is deleted entirely — the new behaviour subsumes both modes, since rollover left the prior open and created nothing while recreate left it open *and* stacked. The #34 vacation-pause guard is also deleted: a sole vacationer's chore now generates as **unclaimed**, so the family can pick it up, and unclaimed occurrences cost nobody points — which is what protects the vacationer, with no special-casing in the generator at all. `pending_review` is exempt from the roll-forward (the work was done and awaits a parent, so the next occurrence is created alongside it, carrying the count unchanged); `actionApprove` then resets the live occurrence's count to 0, and `actionReject` on a superseded occurrence closes it as missed, deducts the points, bumps the live count and **carries the review note forward** so the feedback is actually seen. This replaces the #25 reconciliation, which did the opposite. Streaks now treat `skipped` and `pending_review` as resolved, with `recomputeStreak` rebuilding from the log when a rejection retracts credit. Nightly trigger moved to 3am. New `GET ?action=leaderboard` returns today/week/month totals summed from the log plus all-time from `points_total` (immune to archiving); the old client-side "+N this week" was only ever counting today. Today screen regrouped under One-off / Every day / This week / Monthly and occasional with a matching edge stripe, ordered overdue → due → lead-window with `sort_last` and finished last and points ascending as the tiebreak; the "Due soon" fold and `AssignmentSegments` are deleted, since `lead_days` already decides when a chore becomes visible. Vacation controls moved to the Leaders tab, admin-gated.)* |
| ✅ Done (v1.9.0) | 39 | New feature: a second button on each Manage Chores row to create an assignment of that chore due today, choosing an assignee (or leaving it unclaimed) — for when something needs doing regardless of whether it's scheduled. *(Labelled **Add**, beside Edit, reusing `PersonPicker` with a new `allowUnassigned` option. The backend already existed: `actionAssign` was dispatched but never called from the app. One-offs sit **outside** the recurrence — the generator only ever acts on `assigned_by === 'auto'` rows, so a manual assignment is never closed, rolled forward, penalized, or counted toward the chore's miss streak, and the chore's own schedule is untouched. That's why no frequency restriction is needed: a weekly-Sunday chore given a Tuesday assignment still produces its Sunday one independently. Completing a one-off of an **interval** chore re-anchors the cadence via v1.8.2's completion anchoring, so "do the air filters today" sets the next one 90 days from today. Skip is how you clear an ad-hoc request you've changed your mind about — with no next occurrence, skipping simply removes it.)* |
| ✅ Done (v1.9.0) | 40 | Enhancements: chores that can only be done later in the day (e.g. after dinner) should sink to the bottom of their group; the chore editor should stop silently endangering rotations. *(New `sort_last` boolean on the `Chores` tab with a "Show at the end of its group" checkbox — per-chore rather than per-location, so a chore can still be "Kitchen" and sink; flagged cards carry a small clock icon, chosen over a chip or divider because it marks the card itself and spends none of the colour budget. Separately, `default_assignee` rendered as a single-choice `<select>`, so opening a rotation chore showed the field **blank** and picking anyone replaced the whole list with one person — the location field beside it had a guard for exactly this and the assignee field never got one. It is now **read-only** for rotations, showing "Neal → Claire" plus a hint that rotations are edited in the sheet; every other field still edits normally. Also: History distinguishes "Missed −3" from "Skipped by Rachel", the leaderboard's `Best: 12` reads `Best streak: 12 days`, and `PersonPicker` gained an Unassigned option.)* |
| ✅ Done (v1.9.2) | 41 | Issue: after the v1.9.0 deploy, a daily chore stayed stuck at "due Aug 12, overdue 1d" while the Chores tab read `last_generated_date` = Aug 13 — and re-running the generator did not clear it. Separately, chores solely assigned to someone on vacation never appeared for them on return, and were not in Available to Claim either. *(One root cause. `last_generated_date` is meant to equal the due date of the newest auto occurrence, but the **old** code advanced it in two places without creating a row: the v1.3.0 rollover collapse (advance the cursor, leave the assignment where it is) and the #34 vacation pause (advance the cursor, create nothing). Every chore collapsed or paused on its final old-code run therefore emerged from the migration with the cursor exactly one occurrence **ahead of reality**. `processChoreGeneration` then computed a next occurrence that hadn't come round yet, the appear gate blocked, and it returned *before* reaching the prior-closing logic — stranding the live row as permanently overdue. Re-running could not help: the cursor is the input, so the same wrong answer came out. Daily chores would have self-healed a day later, but a weekly chore would have waited a week, monthly a month, and a 90-day chore three months. Fixed by reconciling the cursor against the rows before using it: if it is ahead of the newest auto occurrence, wind it back. A chore with no assignments at all is deliberately left alone, so blanking the cursor still works as the documented rebuild. Also in this release: ending vacation now recovers every unclaimed chore the person is a default assignee of — sole **or** a rotation member, since turning vacation ON unclaims all their open rows regardless, so a sole-only rule orphaned every rotation chore permanently; and a stranded prior is now closed even when a row for the current occurrence already exists, which the early return used to skip.)* |
| ✅ Done (v1.9.2) | 42 | Visualization tweaks to the Today cards. *(Dropped the assignee chip — the section a card sits in already says whose it is, and the one place that isn't true, Needs review, has its own markup and prints the name itself. Interval chores now read `x180 days` rather than `every 180 days`, short enough to keep the meta row on one line.)* |
| ✅ Done (v1.9.3) | 43 | Issue/enhancement: using **Add** to pull a chore forward — "the mirrors need doing now", or "my kid wants to do one early and get paid for it" — didn't stop the chore coming round again on its original date. Also, the ⋯ menu on a Today card was cut off halfway through "Move date" and never showed Skip. *(The menu was a v1.9.0 regression: the cadence stripe was a child element needing `overflow: hidden` on the card to respect the rounded corners, which clipped the absolutely-positioned dropdown. Now drawn as an inset `box-shadow`, which follows the radius and clips nothing. For the schedule question, **Add** now offers two intents: **One-off** (an extra due today, `assigned_by = manual`, outside the recurrence, schedule untouched) and **Surface early** (the chore's genuine next occurrence surfaced ahead of its appear date, keeping its REAL due date — the same effect as granting that one occurrence extra lead days, marked `auto_early`). Bringing an occurrence forward stamps `last_generated_date` to its due date exactly as the generator would, so the slot is consumed and the chore **cannot re-trigger** on the original date — no completion-time special case is needed, because it simply *is* the occurrence, and roll-forward/miss-counting/points all apply unchanged. `actionToday` widens the reported `lead_days` for `auto_early` rows, without which the row would exist but stay hidden until its real appear date. The picker pre-selects whoever would normally get it (the sole default, or for a rotation whose turn is genuinely next); choosing someone else writes only that assignment and never the chore's default. Daily chores get no choice — they're due today anyway — but Add still works as a plain one-off. Note the Edit form deliberately has no due-date field: it edits the recurring *chore*, and only its individual occurrences have due dates.)* |
| ✅ Done (v1.9.4) | 44 | Change: restore the cadence-based `lead_days` defaults. Leaving the column blank should give bigger jobs more runway rather than making every chore appear only on its due date. *(v1.3.0 shipped frequency defaults — weekly/custom 4, monthly 7, interval `min(N,7)`, daily/once 1 — and v1.3.2 flattened them all to 1 while fixing a short-interval bug where a chore reappeared the next day and was penalized about a day after its due date instead of after a full interval. That bug was actually fixed by the separate `maxLead` cap (lead must be strictly less than the recurrence period), so the defaults are restored via a new `defaultLeadDays()` and the cap keeps the fix. An explicit `lead_days` still wins, still clamped to `1…period−1`; daily and once remain pinned to 1. A weekly chore due Sunday now surfaces on Thursday with no configuration. Note this also means the Today list carries more not-yet-due cards — they sort to the end of their cadence group, after the due and overdue ones.)* |
| ✅ Done (v1.10.0) | 45 | Rework: remove the `custom` frequency, let `daily` be set to specific weekdays, let `interval` chores land on a chosen weekday, and collapse the weekday columns into one. *(The insight: `custom` existed to make something happen ON Monday and Thursday, but it inherited **weekly's** treatment — including a 4-day lead window, which is the opposite of what "on those days" means. So `custom` is folded into `daily`, which keeps its lead pinned at 1: a Mon/Thu chore is a discipline, `weekly` means "sometime this week". New **`weekday_due`** column replaces both `custom_days` and `monthly_weekday` — the old `custom_days` held a bare *number* for weekly but day *names* for custom, two incompatible formats in one column, which is why the sheet was unreadable. `weekday_due` is always comma-separated weekday numbers (0 = Sunday); most frequencies simply use a list of one. Blank on daily means every day. Interval chores can now set it to snap the due date onto the first such weekday after the interval elapses — note the next interval counts from that snapped date, so the schedule drifts slightly later each cycle (chosen deliberately). Also deletes `sortPeriodDays` entirely: cadence grouping keys straight off `frequency` now, which is *more* correct, since a Mon/Thu chore belongs under **Every day** where the old 7÷days arithmetic filed it under This week. Chore editor gains an "Every day / Specific days" toggle for daily and an optional "Due on" weekday for interval. Migration: `migrateWeekdayDue()` converts existing rows once (custom → daily with numeric weekdays, weekly's number moved across, monthly_weekday folded in) and is safe to re-run.)* |
| ✅ Done (v1.10.0) | 46 | Issue: the Leaders tab counted points earned yesterday under **Today**. *(The #31 bug again, in a place the original fix never reached. `actionLeaderboard` bucketed each row by slicing the raw `completed_at` string, while `actionToday` has used `completedOnLocalDate` — which parses the instant and reformats it in the script timezone — since v1.8.1. Legacy `completed_at` values are UTC, so an 8pm Central completion reads as `01:00Z` the following date and a raw slice filed it a day late. Extracted `localDateOf()` from `completedOnLocalDate` and used it for the leaderboard's bucketing, falling back to `due_date` for rows that never had a completion time, such as a missed occurrence carrying its penalty. A sweep for the same pattern found four more: the **approve** path anchored an interval chore's cadence from a sliced `completed_at` (so approving a legacy evening completion shifted the whole schedule a day); **`last_done`** and **History's sort** compared `completed_at` values as raw *strings* across three formats that coexist in a long-lived sheet — legacy UTC, local-offset ISO and date-only — where "…T04:00:00Z" sorts above "…T02:00:00-05:00" despite being two hours *earlier*; and the frontend's `filterTodayAssignments` sliced it too. New `completionInstant()` compares by parsed instant, and `localDateOf()` is now used everywhere a date is derived from a timestamp. Note the windows themselves are unchanged and reset at LOCAL MIDNIGHT — Today at midnight, Week at midnight Sunday, Month at midnight on the 1st — computed per request, so they roll several hours before the 3am chore rollover.)* |
| ✅ Done (v1.10.0) | 47 | Change: let non-admins see the Chores tab, but only to pull a chore onto Today — not to create or alter one. Quick-adds from the Today screen default to needing approval. *(The tab was hidden from non-admins by a `{#if isAdmin}` around the nav link only — the `/admin/chores` route itself was never guarded, so anyone who knew the URL could already open it with every button live. It is now visible to everyone and gates the controls instead: **+ Add**, **Edit** and **Reset rotation** are admin-only, the per-row **Add** stays for everyone, and the title reads "Chores" rather than "Manage Chores". A non-admin's Add offers only themselves as assignee and no Unassigned option, matching what `QuickAddChore` has always done. Separately, `quickAddChore` created its one-off chore with `requires_approval: false`; it is now always `true`, since a quick-add is a chore someone invented for themselves and nothing has vetted that it is real or was done. An admin can clear the flag afterwards in the editor. **Both restrictions are presentation only.** No endpoint checks who is calling — `actionAddChore`/`actionUpdateChore` never have — so this stops a kid using the app, not a kid using the network tab. Enforcing it properly would mean passing the caller's `person_id` on every write and rejecting non-admin callers server-side, which is a bigger change and was deliberately not attempted here. Note also that the per-row Add can still create a one-off of an existing high-value chore, and if that chore doesn't itself require approval the points are awarded without review — the quick-add fix does not close that path.)* |
| ✅ Done (v1.10.0) | 48 | Change: rename the Today cadence group headings to **One-off / Daily / Weekly / Monthly & longer**, and swap daily and weekly's colours. *(The groupings themselves are untouched — labels only. The old set mixed two axes: "One-off" and "Every day" described **how often** a chore returns, while "This week" and "Monthly and occasional" described **when it's due**. The deadline axis can't work on this screen, because everything visible is inside its lead window and therefore due within days — which is why "Monthly and occasional" read as *not now* when in fact those cards are up right now, and why "Today" was rejected as a replacement for "Every day" (other groups are due today too). "Every day" had separately stopped being true once #45 let daily chores be pinned to Mon/Thu. All four labels now sit on the rhythm axis, which is the one the design was already on — "One-off" was never a deadline. Per-card date chips carry the urgency, so the heading doesn't have to. Note "Monthly & longer" is *slightly* optimistic: the group holds every interval chore regardless of length, so a hypothetical every-3-days chore would sit there; "Monthly & interval" is the literally-accurate alternative if short intervals ever get used. Daily is now teal and weekly coral, inverting the original warm-equals-urgent ramp.)* |
